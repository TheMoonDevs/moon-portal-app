{
  "name": "moon-payments-app",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.ts --network",
    "deploy-testnet": "npx hardhat run --network testnet scripts/deploy.ts",
    "deploy_base_sepolia": "npx hardhat run --network base_sepolia scripts/deploy.ts",
    "verify_base_seploia": "npx hardhat verify --network base_sepolia",
    "verify:base": "hardhat verify --network base"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "ethers": "^6.8.0",
    "hardhat": "^2.3.0",
    "hardhat-gas-reporter": "^1.0.8",
    "solidity-coverage": "^0.8.1",
    "ts-node": ">=8.0.0"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@types/chai": "^4.2.0",
    "@types/mocha": ">=9.1.0",
    "@types/node": "^20.12.7",
    "chai": "^4.3.4",
    "dotenv": "^16.4.5",
    "eslint": "^8",
    "eslint-config-next": "14.1.0",
    "typescript": "^5",
    "webpack": "^5.90.3"
  }
}
